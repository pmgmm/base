/*     let obj_notification = new NotificationWrapper();
    obj_notification.permit();
    obj_notification.send(); */

/*
        //await TASK.syncToServer();
        await TASK.syncFromServer();
        await TASK.load();
 */


/* const filter = [2,45,17];
const fieldToFilter = "someField";
objectStore.getAll().onsuccess = function(evt) {
  const filtered = evt.target.result.filter(i => filter.includes(i[fieldToFilter]));
  console.log(filtered);
}; */

